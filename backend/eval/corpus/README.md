Eval corpus. The PDFs are not committed; fetch them with:

    curl -L -o attention.pdf https://arxiv.org/pdf/1706.03762
    curl -L -o bert.pdf      https://arxiv.org/pdf/1810.04805

`questions.json` gold labels were derived from these exact files. Replacing them
with different versions will invalidate the page numbers.
